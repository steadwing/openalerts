{
  "private": true,
  "name": "openalerts",
  "description": "OpenAlerts — Real-time monitoring & alerting for AI agent frameworks",
  "workspaces": [
    "node",
    "dashboard"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "build:node": "npm run build --workspace=node",
    "build:dashboard": "npm run build --workspace=dashboard",
    "typecheck": "npm run typecheck --workspace=node",
    "clean": "npm run clean --workspaces --if-present"
  }
}
